package com.cboe.client.util.collections;

/**
 * IntLongArrayHolder.java
 *
 * @author Dmitry Volpyansky
 *
 * FILE GENERATED BY VELOCITY TEMPLATE ENGINE FROM /vobs/dte/client/generator/DV_XYArrayHolder.java (KEY_TYPE=int, VALUE_TYPE=long)
 *
 */

import com.cboe.client.util.*;

public class IntLongArrayHolder implements IntLongArrayHolderIF
{
    public int[]   keys;
    public long[] values;
    public int      size;

    public static final IntLongArrayHolderIF EmptyArrayHolder = new IntLongArrayHolder()
    {
        public void add(int key, long value) {}
    };

    public IntLongArrayHolder()
    {
        this(0);
    }

    public IntLongArrayHolder(int capacity)
    {
        keys   = new int[capacity];
        values = new long[capacity];
    }

    public int size()
    {
        return size;
    }

    public boolean isEmpty()
    {
        return size == 0;
    }

    public int capacity()
    {
        return keys.length;
    }

    public int ensureCapacity(int capacity)
    {
        if (keys.length < capacity)
        {
            keys   = CollectionHelper.arrayclone(keys,   0, keys.length,   capacity);
            values = CollectionHelper.arrayclone(values, 0, values.length, capacity);
        }

        return keys.length;
    }

    public int[] keys()
    {
        return keys;
    }

    public int[] getKeys()
    {
        return CollectionHelper.arrayclone(keys, size);
    }

    public long[] values()
    {
        return values;
    }

    public long[] getValues()
    {
        return CollectionHelper.arrayclone(values, size);
    }

    public void add(int key, long value)
    {
        int newSize = size + 1;

        if (newSize >= keys.length)
        {
            keys   = CollectionHelper.arrayclone(keys,   0, keys.length,   newSize);
            values = CollectionHelper.arrayclone(values, 0, values.length, newSize);
        }

        keys[size]   = key;
        values[size] = value;

        size = newSize;
    }

    public int getKey(int index)
    {
        if (index < size)
        {
            return keys[index];
        }

        return IntegerHelper.INVALID_VALUE;
    }

    public long getValue(int index)
    {
        if (index < size)
        {
            return values[index];
        }

        return IntegerHelper.INVALID_VALUE;
    }

    public boolean containsKey(int key)
    {
        for (int i = 0; i < size; i++)
        {
            if (keys[i] == key)
            {
                return true;
            }
        }

        return false;
    }

    public boolean containsValue(long value)
    {
        for (int i = 0; i < size; i++)
        {
            if (values[i] == value)
            {
                return true;
            }
        }

        return false;
    }

    public IntLongVisitorIF acceptVisitor(IntLongVisitorIF visitor)
    {
        for (int i = 0; i < size; i++)
        {
            visitor.visit(keys[i], values[i]);
        }

        return visitor;
    }

    public IntLongArrayHolderIF clear()
    {
        for (int i = 0; i < size; i++)
        {
            keys[i]   = 0;
            values[i] = 0;
        }

        size = 0;

        return this;
    }
}

