package com.cboe.client.util.collections;

/**
 * IntStringArrayHolder.java
 *
 * @author Dmitry Volpyansky
 *
 * FILE GENERATED BY VELOCITY TEMPLATE ENGINE FROM /vobs/dte/client/generator/DV_XYArrayHolder.java (KEY_TYPE=int, VALUE_TYPE=String)
 *
 */

import com.cboe.client.util.*;

public class IntStringArrayHolder implements IntStringArrayHolderIF
{
    public int[]   keys;
    public String[] values;
    public int      size;

    public static final IntStringArrayHolderIF EmptyArrayHolder = new IntStringArrayHolder()
    {
        public void add(int key, String value) {}
    };

    public IntStringArrayHolder()
    {
        this(0);
    }

    public IntStringArrayHolder(int capacity)
    {
        keys   = new int[capacity];
        values = new String[capacity];
    }

    public int size()
    {
        return size;
    }

    public boolean isEmpty()
    {
        return size == 0;
    }

    public int capacity()
    {
        return keys.length;
    }

    public int ensureCapacity(int capacity)
    {
        if (keys.length < capacity)
        {
            keys   = CollectionHelper.arrayclone(keys,   0, keys.length,   capacity);
            values = CollectionHelper.arrayclone(values, 0, values.length, capacity);
        }

        return keys.length;
    }

    public int[] keys()
    {
        return keys;
    }

    public int[] getKeys()
    {
        return CollectionHelper.arrayclone(keys, size);
    }

    public String[] values()
    {
        return values;
    }

    public String[] getValues()
    {
        return CollectionHelper.arrayclone(values, size);
    }

    public void add(int key, String value)
    {
        int newSize = size + 1;

        if (newSize >= keys.length)
        {
            keys   = CollectionHelper.arrayclone(keys,   0, keys.length,   newSize);
            values = CollectionHelper.arrayclone(values, 0, values.length, newSize);
        }

        keys[size]   = key;
        values[size] = value;

        size = newSize;
    }

    public int getKey(int index)
    {
        if (index < size)
        {
            return keys[index];
        }

        return IntegerHelper.INVALID_VALUE;
    }

    public String getValue(int index)
    {
        if (index < size)
        {
            return values[index];
        }

        return null;
    }

    public boolean containsKey(int key)
    {
        for (int i = 0; i < size; i++)
        {
            if (keys[i] == key)
            {
                return true;
            }
        }

        return false;
    }

    public boolean containsValue(String value)
    {
        for (int i = 0; i < size; i++)
        {
            if (values[i] == value || value.equals(values[i]))
            {
                return true;
            }
        }

        return false;
    }

    public IntObjectVisitorIF acceptVisitor(IntObjectVisitorIF visitor)
    {
        for (int i = 0; i < size; i++)
        {
            visitor.visit(keys[i], values[i]);
        }

        return visitor;
    }

    public IntStringArrayHolderIF clear()
    {
        for (int i = 0; i < size; i++)
        {
            keys[i]   = 0;
            values[i] = null;
        }

        size = 0;

        return this;
    }
}

