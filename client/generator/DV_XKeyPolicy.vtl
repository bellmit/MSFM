#if ( ${KEY_TYPE} == "Int" )
#set ( $KEY_TYPE = "int" )
#elseif ( ${KEY_TYPE} == "Long" )
#set ( $KEY_TYPE = "long" )
#end
#if ( ${KEY_TYPE} == "int" )
    #set ( $__dont_fill_in_key_name           = "Int" )
#elseif ( ${KEY_TYPE} == "long" )
    #set ( $__dont_fill_in_key_name           = "Long" )
#else
    #set ( $__dont_fill_in_key_name           = "${KEY_TYPE}" )
#end
package com.cboe.client.util.collections;

/**
 * ${__dont_fill_in_key_name}KeyPolicy.java
 *
 * @author Dmitry Volpyansky
 *
 * FILE GENERATED BY VELOCITY TEMPLATE ENGINE FROM /vobs/dte/client/generator/DV_XYKeyPolicy.java (KEY_TYPE=${KEY_TYPE})
 *
 */

public class ${__dont_fill_in_key_name}KeyPolicy implements ${__dont_fill_in_key_name}KeyPolicyIF
{
    public boolean canInsert(${KEY_TYPE} key)
    {
        return true;
    }
    public boolean canRemove(${KEY_TYPE} key)
    {
        return true;
    }
}
