package com.cboe.cfix.cas.casLogin;
/*
import org.junit.Test;
import static org.junit.Assert.assertTrue;
import static org.mockito.Mockito.*;
import com.cboe.interfaces.cfix.CfixCasLogin;
import com.cboe.exceptions.SystemException;
import com.cboe.exceptions.CommunicationException;
import com.cboe.idl.cmi.UserSessionManager;
import com.cboe.idl.cmiV4.UserSessionManagerV4;
import com.cboe.idl.cmiUser.UserLogonStruct;
import com.cboe.idl.cmiCallback.CMIUserSessionAdmin;
*/

/**
 * Created by IntelliJ IDEA.
 * User: lip
 * Date: May 12, 2010
 * Time: 4:02:32 PM
 * To change this template use File | Settings | File Templates.
 */
public class CfixCasLoginImplTest {
/*
    @Test
    public void casLoginTest()
    {
        try{

            UserSessionManagerV4 mySessionManager = mock(UserSessionManagerV4.class);
            //CASAccessManager myCASAccessManager = mock(CASAccessManager.class);
            //when(myCASAccessManager.getUserSessionManagerV4()).thenReturn(mySessionManager);
            CfixCasLoginImpl myCasLogin = mock(CfixCasLoginImpl.class);
            when(myCasLogin.loginToCas(anyString(), anyInt(), anyString(), anyString())).thenReturn(mySessionManager);

            when(System.getProperty("Cfix.CasIP")).thenReturn("dev3cas");
            when(System.getProperty("Cfix.CasPort")).thenReturn("8003");
            when(System.getProperty("Cfix.CasUserID")).thenReturn("CD168");
            when(System.getProperty("Cfix.CasPassword")).thenReturn("CD168");

            when(System.getProperty("Cfix.BackupCasIP")).thenReturn("dev4cas");
            when(System.getProperty("Cfix.BackupCasPort")).thenReturn("8003");
            when(System.getProperty("Cfix.BackupCasUserID")).thenReturn("X02");
            when(System.getProperty("Cfix.BackupCasPassword")).thenReturn("X02");

            myCasLogin.init();

            assertTrue(myCasLogin.isLoggedinToCas());
            myCasLogin.logoutCas();
            assertTrue(!myCasLogin.isLoggedinToCas());
        }catch(Exception e){
            e.printStackTrace();
        }
    }
    */
}
